
import React from 'react'

function ErrorComponent(props) {
    return (
        <div>
            {props.abc}
        </div>
    )
}

export default ErrorComponent


//const ErrorComponent = (props) => <div>{props.abc}</div>